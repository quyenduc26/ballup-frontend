/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["example.com","encrypted-tbn0.gstatic.com", "foba.vn","co-nhan-tao.com","cloudflare-ipfs.com"], // Thay bằng domain thật của ảnh
      },
};

module.exports = nextConfig;
